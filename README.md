# Project-5
This project implements a two-tier client-server database application developed in Java using JDBC and MySQL. The system provides a graphical user interface that allows authenticated users to execute SQL commands against a backend database.
